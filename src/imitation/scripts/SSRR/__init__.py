"""Self-Supervised Reward Regression (SSRR) implementation (local to imitation).

This package is intentionally self-contained under `imitation.scripts.SSRR` so we
can validate SSRR in isolation (double-integrator first) before integrating it
into the NTRIL pipeline as an alternative to D-REX.
"""

